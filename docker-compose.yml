# Docker Compose para servicios locales
# Uso: docker-compose up -d
#
# PostgreSQL (siempre):
# - Host: localhost
# - Puerto: 5533
# - Usuario: postgres
# - Password: postgres
# - Base de datos: nahuel-boxer-db-boxer-db
#
# MinIO (solo si STORAGE_PROVIDER=s3):
# - API S3: http://localhost:9002
# - Console: http://localhost:9003
# - Usuario: minioadmin
# - Password: minioadmin123
#
# Para levantar solo PostgreSQL:
#   docker-compose up -d db
#
# Para levantar PostgreSQL + MinIO:
#   docker-compose --profile storage up -d
# Para Bajar
# docker-compose --profile storage down

name: nahuel-boxer

services:
  db:
    image: postgres:16-alpine
    container_name: nahuel-boxer-db
    restart: "no"
    ports:
      - '5533:5432'
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-nahuel-boxer-db-boxer-db}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres']
      interval: 5s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:latest
    container_name: nahuel-boxer-minio
    profiles:
      - storage
    restart: "no"
    ports:
      - '9002:9000'   # API S3
      - '9003:9001'   # Console Web
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin123}
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"
    healthcheck:
      test: ['CMD', 'mc', 'ready', 'local']
      interval: 5s
      timeout: 5s
      retries: 5

  # Servicio para crear el bucket inicial automáticamente
  minio-init:
    image: minio/mc:latest
    container_name: nahuel-boxer-minio-init
    profiles:
      - storage
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      mc alias set myminio http://minio:9000 $${MINIO_ROOT_USER:-minioadmin} $${MINIO_ROOT_PASSWORD:-minioadmin123};
      mc mb myminio/$${S3_BUCKET:-nahuel-boxer-docs-boxer-docs-docs-docs} --ignore-existing;
      mc anonymous set download myminio/$${S3_BUCKET:-documents}/public;
      echo 'Bucket created successfully';
      exit 0;
      "
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin123}
      S3_BUCKET: ${S3_BUCKET:-documents}

volumes:
  postgres_data:
  minio_data:
